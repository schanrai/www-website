---
interface Props {
  color?: string;
  class?: string;
}
const { color, class: className = '' } = Astro.props;
const style = color ? `color: ${color}` : '';
---

<p class:list={['section-label', className]} style={style}>
  <slot />
</p>
